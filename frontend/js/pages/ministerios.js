// Ministério foi removido do modelo v2.
// Este arquivo é mantido apenas para não quebrar imports legados.
export async function mount(container) {
  container.innerHTML = '<div class="empty-state">Módulo removido do sistema.</div>';
}
